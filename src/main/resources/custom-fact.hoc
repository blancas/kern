(* Define the factorial function. *)

Func factorial() {
    IF ($1 == 0) {
        Return 1
    } else {
        RETURN $1 * factorial($1 - 1)
    }
}

(* Oh, no, can't do that!

   (* Call factorial on a negative *)

   factorial(-100)
 
*)

(* Call the factorial *)

FACTorial(6)
